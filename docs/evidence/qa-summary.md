# Local QA evidence

Status: author remediation evidence after a distinct `REVISE` verdict at `afb169ff`; fresh distinct review pending on the new immutable head.

## Failing before

Command: `npm test -- --run tests/adoption-engine.test.ts`

Exit: `1`.

Observed failure: the test runner could not resolve `lib/domain/adoption-engine` because the rules engine did not exist. This is implementation-before evidence, not mutation proof.

## Passing after

| Gate                      | Command                        | Result                                                                    |
| ------------------------- | ------------------------------ | ------------------------------------------------------------------------- |
| Unit and contract tests   | `npm test`                     | 5 files, 29/29 passed                                                     |
| Critical mutation control | `npm run test:mutation`        | 5 disabled detectors failed bound negatives; restored source passed twice |
| TypeScript                | `npm run typecheck`            | passed, exit 0                                                            |
| ESLint                    | `npm run lint`                 | passed with zero warnings, exit 0                                         |
| Production build          | `npm run build`                | Next.js 16.2.12 compiled and generated all 12 pages/routes, exit 0        |
| Generated-file stability  | pre/post build hashes          | `next-env.d.ts` and `tsconfig.json` hashes unchanged                      |
| Browser journey           | `npm run test:e2e`             | 2/2 passed                                                                |
| Accessibility             | primary browser journey        | zero serious/critical axe violations                                      |
| Mobile layout             | 390 px browser journey         | zero page-level horizontal overflow                                       |
| Dependency audit          | `npm audit --audit-level=high` | zero vulnerabilities                                                      |
| Secret scan               | `npm run scan:secrets`         | 4 detectors proved; 65 repository files scanned; zero findings            |

Critical mutation evidence is generated at `evidence/qa/mutation-control.txt`. The test-only control independently disables record-integrity, current-release, current-usage, repeat-use, and customer-validation detectors; each bound negative assertion must fail. It restores the exact source in `finally` and requires two consecutive passing runs. No runtime kill switch is shipped.

Browser proof covers keyboard opening, focus containment, Escape close, focus restoration, exact-record drill-down from all four counters, the keyboard clinic receipt journey, a serious/critical axe scan, and the 390 px page-overflow check. Screenshots are generated by the E2E run so they are reproducible.

Two initial E2E attempts were blocked before test execution by a transient local port collision at 3100. A manually proven free port (3199) replaced it. The first executable browser run then found a real mobile accessibility defect: the visible navigation lacked its own accessible label. The navigation was labelled and the final browser run passed 2/2.
